export const environment = {
  production: false,
  /**
   * For local verification keep this empty so URLs use the current origin.
   * Set explicitly if your API/static host is on a different public domain.
   */
  publicBaseUrl: 'https://localhost:7052',
  emailImagesPublicPath: '/assets/images'
};
